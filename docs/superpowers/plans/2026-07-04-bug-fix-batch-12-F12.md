# Bug Fix Batch 12 · Finding 12 · v5_position_close 支持 LIVE · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `POST /api/v5/positions/{position_id}/close` 端点先查 paper_trades,不存在则查 positions_v5;LIVE 命中时用 `get_trader()` + `V5PositionManager.close_position()`。响应加 `mode: "paper" | "live"` 字段。

**Architecture:** 单 task TDD:先写 3 tests(RED —— 老代码只查 paper 表)→ 重写端点 + 加 `mode` 字段 → GREEN → 单 commit。

**Tech Stack:** Python stdlib + FastAPI TestClient + pytest tmp_path + monkeypatch + unittest.mock。无新增 pip 依赖。

## Global Constraints

- Only 2 files touched:
  - `api/routes/v5_position_close.py` (重写端点 + Response 加 mode)
  - `tests/test_v5_position_close.py` (create, 3 tests)
- Response schema: `mode: str = "paper"` 追加(default backward-compat)
- 端点主流程:
  1. 双表查询 paper → live
  2. 都无 → 404
  3. paper 命中 → PaperPositionManager.close_position + `mode="paper"`
  4. live 命中 → get_trader() 失败 → 503;V5PositionManager.close_position 抛异常 → 502;成功 → re-read status + `mode="live"`
- LIVE 分支 catch 顺序:`get_trader` 单独 catch → 503;`V5PositionManager(...)` + `close_position` 组合 catch → 502
- `get_trader()` 返 None 也算 503(monitor bootstrap 有相同判断)
- Mock trader:`mock_trader.close_position(symbol)` 需 return `{"success": True}`(V5PositionManager 检查 result dict)
- 不改 PaperPositionManager / V5PositionManager 内部
- 不改前端
- 现有 tests 无回归
- Single commit, subject EXACT: `fix(v5_position_close): 支持 LIVE 分支 + 响应加 mode 字段 (Finding 12)`

---

## File Structure

| 路径 | 动作 |
|---|---|
| `api/routes/v5_position_close.py` | Modify —— 双表查询 + LIVE 分支 + mode 字段 |
| `tests/test_v5_position_close.py` | Create —— 3 tests |

---

# Task 1: 端点重写 + Response 扩展 + 3 tests

**Files:**
- Modify: `api/routes/v5_position_close.py`
- Create: `tests/test_v5_position_close.py`

**Interfaces:**
- Consumes: pytest `monkeypatch` + `tmp_path`,`fastapi.testclient.TestClient`,`unittest.mock.MagicMock` + `patch`
- Produces:
  - `CloseResponse.mode: str` 新字段(default "paper")
  - 端点新支持 LIVE 分支;错误码变化:404 保留、409 保留、新加 502/503

## RED phase

- [ ] **Step 1: 创建 `tests/test_v5_position_close.py`(3 tests)**

```python
"""Batch 12 Finding 12: v5_position_close 支持 LIVE 分支。"""
import sqlite3
from unittest.mock import MagicMock, patch

from fastapi.testclient import TestClient


def _init_schema(db_path):
    """建 paper_trades + positions_v5 最小 schema。"""
    conn = sqlite3.connect(db_path)
    conn.executescript('''
        CREATE TABLE paper_trades (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            symbol TEXT, side TEXT, status TEXT, entry_price REAL,
            entry_time TEXT, exit_price REAL, exit_time TEXT, exit_reason TEXT,
            pnl REAL, pnl_percent REAL, holding_hours REAL,
            current_price REAL, stop_loss REAL, take_profit REAL,
            position_size_usdt REAL, leverage INTEGER,
            strategy_id TEXT, created_at TEXT, updated_at TEXT,
            source_score_id INTEGER
        );
        CREATE TABLE positions_v5 (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            symbol TEXT, side TEXT, status TEXT, entry_price REAL,
            entry_time TEXT, sl_price REAL, tp_price REAL,
            sl_attached INTEGER, tp_attached INTEGER, error_context TEXT,
            size_usdt REAL, leverage INTEGER, position_size_coins REAL,
            target_close_at TEXT, extension_count INTEGER,
            created_at TEXT, updated_at TEXT,
            exit_price REAL, exit_time TEXT, exit_reason TEXT,
            pnl_realized REAL
        );
    ''')
    conn.commit()
    conn.close()


def test_close_paper_position_still_works(monkeypatch, tmp_path):
    """paper_trades 命中 → mode=paper, status=CLOSED(F12 不 regress paper 路径)。"""
    db_path = tmp_path / "test.db"
    _init_schema(str(db_path))
    conn = sqlite3.connect(str(db_path))
    conn.execute(
        "INSERT INTO paper_trades (id, symbol, side, status, entry_price, entry_time) "
        "VALUES (1, 'BTC/USDT', 'LONG', 'OPEN', 50000, datetime('now'))"
    )
    conn.commit()
    conn.close()
    monkeypatch.setenv("DB_PATH", str(db_path))

    from api.main import app
    client = TestClient(app)
    r = client.post("/api/v5/positions/1/close",
                    json={"exit_price": 51000, "exit_reason": "MANUAL_USER"})
    assert r.status_code == 200
    body = r.json()
    assert body["mode"] == "paper"
    assert body["position_id"] == 1


def test_close_live_position_uses_broker(monkeypatch, tmp_path):
    """positions_v5 命中 → 调 get_trader + V5PositionManager, mode=live。"""
    db_path = tmp_path / "test.db"
    _init_schema(str(db_path))
    conn = sqlite3.connect(str(db_path))
    conn.execute(
        "INSERT INTO positions_v5 (id, symbol, side, status, entry_price, entry_time, "
        "size_usdt, leverage, position_size_coins) "
        "VALUES (100, 'BTC/USDT', 'LONG', 'OPEN', 50000, datetime('now'), 15, 10, 0.0003)"
    )
    conn.commit()
    conn.close()
    monkeypatch.setenv("DB_PATH", str(db_path))

    mock_trader = MagicMock()
    mock_trader.close_position = MagicMock(return_value={"success": True})

    with patch("scripts.exchange_factory.get_trader", return_value=mock_trader):
        from api.main import app
        client = TestClient(app)
        r = client.post("/api/v5/positions/100/close",
                        json={"exit_price": 51000, "exit_reason": "MANUAL_USER"})

    assert r.status_code == 200
    body = r.json()
    assert body["mode"] == "live"
    assert body["position_id"] == 100
    mock_trader.close_position.assert_called_once_with("BTC/USDT")


def test_close_position_not_found_returns_404(monkeypatch, tmp_path):
    """paper + live 都无 → 404。"""
    db_path = tmp_path / "test.db"
    _init_schema(str(db_path))
    monkeypatch.setenv("DB_PATH", str(db_path))

    from api.main import app
    client = TestClient(app)
    r = client.post("/api/v5/positions/999/close",
                    json={"exit_price": 51000, "exit_reason": "MANUAL_USER"})
    assert r.status_code == 404
```

- [ ] **Step 2: 跑 tests —— 期望 RED**

```bash
python3 -m pytest tests/test_v5_position_close.py -v
```

Expected:
- test 1 (paper) 可能 PASS 或 FAIL: 老代码走 paper 路径 OK,但会返 `mode` 缺失 → pydantic 拒绝 → 500 或 `mode` 断言失败
- test 2 (live) FAIL: 老代码只查 paper,positions_v5 命中不到 → 404,mock 未被调用
- test 3 (404) PASS: 老代码也返 404

至少 test 2 FAIL 就够 RED signal。

## GREEN phase

- [ ] **Step 3: 修改 `api/routes/v5_position_close.py` Response**

anchor unique: `class CloseResponse(BaseModel):`

**Before**:
```python
class CloseResponse(BaseModel):
    position_id: int
    status: str
    exit_price: float
    exit_reason: str
```

**After**:
```python
class CloseResponse(BaseModel):
    position_id: int
    status: str
    exit_price: float
    exit_reason: str
    mode: str = "paper"  # "paper" | "live"
```

- [ ] **Step 4: 重写端点主体**

用 Edit,anchor unique 是完整现有函数体(L34-69),用 `@router.post("/positions/{position_id}/close", response_model=CloseResponse)` 开头唯一。

**Before**(整个 close_position 函数):
```python
@router.post("/positions/{position_id}/close", response_model=CloseResponse)
async def close_position(
    position_id: int = Path(...),
    body: CloseRequest = ...,
) -> CloseResponse:
    db = _db()
    conn = sqlite3.connect(db)
    try:
        row = conn.execute(
            "SELECT status FROM paper_trades WHERE id=?", (position_id,)
        ).fetchone()
    finally:
        conn.close()

    if row is None:
        raise HTTPException(status_code=404, detail=f"position {position_id} not found")
    if (row[0] or "").upper() == "CLOSED":
        raise HTTPException(status_code=409, detail=f"position {position_id} already CLOSED")

    from scripts.paper_position_manager import PaperPositionManager
    pm = PaperPositionManager(db_path=db)
    pm.close_position(position_id, exit_price=body.exit_price, exit_reason=body.exit_reason)

    # Reflection 入队(跟 v5_position_monitor 的 close 路径对齐)
    try:
        from scripts.local_db import enqueue_reflection
        enqueue_reflection(position_id, db_path=db)
    except Exception as e:
        print(f"[v5_position_close] reflection enqueue failed: {e}")

    return CloseResponse(
        position_id=position_id,
        status="CLOSED",
        exit_price=body.exit_price,
        exit_reason=body.exit_reason,
    )
```

**After**:
```python
@router.post("/positions/{position_id}/close", response_model=CloseResponse)
async def close_position(
    position_id: int = Path(...),
    body: CloseRequest = ...,
) -> CloseResponse:
    db = _db()
    paper_status: Optional[str] = None
    live_status: Optional[str] = None
    conn = sqlite3.connect(db)
    try:
        row = conn.execute(
            "SELECT status FROM paper_trades WHERE id=?", (position_id,)
        ).fetchone()
        if row is not None:
            paper_status = row[0]
        else:
            row = conn.execute(
                "SELECT status FROM positions_v5 WHERE id=?", (position_id,)
            ).fetchone()
            if row is not None:
                live_status = row[0]
    finally:
        conn.close()

    if paper_status is None and live_status is None:
        raise HTTPException(status_code=404, detail=f"position {position_id} not found")

    if paper_status is not None:
        # ── paper 分支 ─────────────────────────────────
        if (paper_status or "").upper() == "CLOSED":
            raise HTTPException(status_code=409, detail=f"position {position_id} already CLOSED")
        from scripts.paper_position_manager import PaperPositionManager
        pm = PaperPositionManager(db_path=db)
        pm.close_position(position_id, exit_price=body.exit_price, exit_reason=body.exit_reason)

        # Reflection 入队(跟 v5_position_monitor 的 close 路径对齐)
        try:
            from scripts.local_db import enqueue_reflection
            enqueue_reflection(position_id, db_path=db)
        except Exception as e:
            print(f"[v5_position_close] reflection enqueue failed: {e}")

        return CloseResponse(
            position_id=position_id, status="CLOSED",
            exit_price=body.exit_price, exit_reason=body.exit_reason,
            mode="paper",
        )

    # ── LIVE 分支 ───────────────────────────────────
    if (live_status or "").upper() == "CLOSED":
        raise HTTPException(status_code=409, detail=f"position {position_id} already CLOSED")
    try:
        from scripts.exchange_factory import get_trader
        trader = get_trader()
    except Exception as e:
        raise HTTPException(
            status_code=503,
            detail=f"broker unavailable: {type(e).__name__}: {e}",
        )
    if trader is None:
        raise HTTPException(status_code=503, detail="broker unavailable: get_trader() returned None")
    try:
        from scripts.v5_position_manager import V5PositionManager
        live_pm = V5PositionManager(broker=trader, db_path=db)
        live_pm.close_position(
            position_id, exit_price=body.exit_price, exit_reason=body.exit_reason,
        )
    except Exception as e:
        raise HTTPException(
            status_code=502,
            detail=f"live close failed: {type(e).__name__}: {e}",
        )
    # re-read: broker 结果决定 status(CLOSED / OPEN / ERROR_RECONCILE_NEEDED)
    conn = sqlite3.connect(db)
    try:
        r = conn.execute(
            "SELECT status FROM positions_v5 WHERE id=?", (position_id,)).fetchone()
    finally:
        conn.close()
    final_status = (r[0] if r else "UNKNOWN") or "UNKNOWN"
    return CloseResponse(
        position_id=position_id, status=final_status,
        exit_price=body.exit_price, exit_reason=body.exit_reason,
        mode="live",
    )
```

- [ ] **Step 5: 跑 tests —— 期望 GREEN**

```bash
python3 -m pytest tests/test_v5_position_close.py -v
```

Expected: 3/3 PASS

若失败:
- test 1 FAIL "mode not in response" → 检查 Response 新字段是否加了 default
- test 2 mock 未被调用 → 检查 `patch("scripts.exchange_factory.get_trader", ...)` 路径是否精确
- test 2 500 → 检查 `V5PositionManager(broker=trader, db_path=db)` 构造是否兼容 mock
- test 2 status 不是 "OPEN"(mock 没 update DB): 因 broker 成功但 V5PositionManager._update_closed 需要 tp_price/sl_price/pnl_realized 等,若 mock 不完整可能抛 —— 检查 mock 是否 return 完整 dict + positions_v5 schema 是否含所需列
- test 3 200 而非 404 → 检查双表查询逻辑

**注意**:test 2 中,V5PositionManager._update_closed(内部) 需要 positions_v5 有 `exit_price`、`exit_time`、`exit_reason`、`pnl_realized` 列,`_init_schema` 中已建。若 mock 成功返 `{"success": True}`,V5PositionManager 走 `_update_closed` → UPDATE positions_v5 → status=CLOSED。re-read 应返 "CLOSED"。若 test 断言 `status="CLOSED"` 应对应此路径。当前 test 只断言 `mode="live"`,更宽松 —— 不严格看 status,允许 mock 底层写 DB 不完整。

## 邻近回归 + sanity + commit

- [ ] **Step 6: 邻近回归**

```bash
python3 -m pytest tests/test_v5_position_close.py tests/test_paper_position_manager_v5.py -v 2>&1 | tail -15
```

Expected: 9/9 pass (3 new + 6 F11 baseline)

- [ ] **Step 7: sanity greps**

```bash
grep -c "positions_v5" api/routes/v5_position_close.py
# 期望: ≥1(至少 SELECT 那一处)

grep -c "get_trader\|V5PositionManager" api/routes/v5_position_close.py
# 期望: 2(import + 构造)

grep -c '"mode"\|mode=' api/routes/v5_position_close.py
# 期望: ≥3(schema default + paper return + live return)

grep -c "status_code=502\|status_code=503" api/routes/v5_position_close.py
# 期望: ≥3(502 + 503 两处 + 503 None trader)
```

- [ ] **Step 8: Commit**

```bash
git add api/routes/v5_position_close.py tests/test_v5_position_close.py
git commit -m "$(cat <<'EOF'
fix(v5_position_close): 支持 LIVE 分支 + 响应加 mode 字段 (Finding 12)

修 bug-fix-list.md Finding 12 (P1):端点原只查 paper_trades,LIVE 仓位
(positions_v5)请求返 404,前端 useV5ClosePosition 对两类仓位统一调该 URL,
LIVE 模式手动平仓完全失效,交易所仍持有真实仓位且 SL/TP 挂单。

Change:
- Response 加 mode 字段("paper" | "live", default "paper" backward-compat)
- 端点先查 paper_trades → 未命中查 positions_v5
- paper 命中: 现有 PaperPositionManager 流程 + mode="paper"
- live 命中: get_trader() 失败 → 503; V5PositionManager.close_position 抛 → 502;
  成功 → re-read positions_v5.status 反映 broker 结果(CLOSED/OPEN/ERROR_RECONCILE_NEEDED)
- 都无 → 404

Tests:
- 新增 tests/test_v5_position_close.py 3 tests
  - paper 命中: mode=paper 且 status=200(F12 不 regress paper)
  - live 命中: mock get_trader + 断言 broker.close_position 被调用一次, mode=live
  - 都无: 404

超范围声明:paper/live id 冲突不处理(生产极小概率); 前端 useV5ClosePosition 不改
(mode 新字段 additive)。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

# Self-Review 记录

- **spec § 四 Change 1 (Response mode)**: Step 3 ✓
- **spec § 五 Change 2 (端点重写)**: Step 4 ✓
- **spec § 六 Change 3 (3 tests)**: Step 1 ✓
- **spec § 七 验收**: Step 5 (3/3) + Step 6 (回归) + Step 7 (sanity) ✓
- **spec § 八 失效模式**: 已声明 id 冲突 + 503 / 502 分层 + RETRYABLE 状态透传 ✓
- **placeholder scan**: 无 TBD ✓
- **type consistency**: `mode` 字段 / 502 / 503 / mock 路径 spec + plan + test 一致 ✓
- **测试 RED→GREEN**: Step 2 = RED(test 2 fail)；Step 5 = GREEN ✓
- **atomicity**: 单 commit at Step 8 ✓
