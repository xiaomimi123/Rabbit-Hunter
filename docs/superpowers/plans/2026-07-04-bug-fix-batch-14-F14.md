# Bug Fix Batch 14 · Finding 14 · LIVE exit_price 用 broker 实际成交价 · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `V5PositionManager.close_position()` 成功平仓时从 `broker.close_position()` 返回值的 `price` 字段取实际成交价写 DB;所有 CLOSED / PERMANENT 补记账在 `positions_v5.error_context` 标记 `exit_price_source` ("broker_fill" | "monitor_tick" | "monitor_tick_permanent")。

**Architecture:** 单 task TDD:先写 3 tests(RED —— 老代码永远用 caller 传入的 exit_price 且 error_context 无 source)→ `_update_closed` 加 source 参数 + `close_position` 两分支填 source → GREEN → 单 commit。

**Tech Stack:** Python stdlib(json, sqlite3)+ pytest tmp_path + unittest.mock。无新增 pip 依赖。

## Global Constraints

- Only 2 files touched:
  - `scripts/v5_position_manager.py` (modify `_update_closed` + `close_position`)
  - `tests/test_v5_position_manager.py` (追加 3 tests,复用 `_seeded_open_position` helper)
- `_update_closed` 新参数 EXACT: `exit_price_source: str = "monitor_tick"`(default,backward-compat 其他 caller 无需改)
- ctx 里 EXACT key: `"exit_price_source"`
- 3 值 EXACT:`"broker_fill"` | `"monitor_tick"` | `"monitor_tick_permanent"`
- 成功分支 broker price 解析:`float(rb_result.get("price"))`,try/except (TypeError, ValueError) 兜 fallback caller
- ctx_json 从此永远非 None(至少含 source)—— `json.dumps(ctx)` 无条件
- 不改 monitor 路径 / paper 路径 / broker 内部 / PnL 计算 / 前端
- 不加 `fetch_my_trades` / `fetch_orders` 查询
- 现有 tests 无回归:
  - `test_v5_position_manager.py` 5 existing → 8/8(加 3)
  - `test_v5_position_close.py` 3/3 (F12 baseline)
- Single commit, subject EXACT: `fix(v5_position_manager): close 用 broker 实际成交价 + error_context 标 exit_price_source (Finding 14)`

---

## File Structure

| 路径 | 动作 |
|---|---|
| `scripts/v5_position_manager.py` | Modify —— `_update_closed` 加 source 参数 + `close_position` 两分支传 source |
| `tests/test_v5_position_manager.py` | Modify —— 追加 3 tests |

---

# Task 1: source 标签 + broker 实际成交价 + 3 tests

**Files:**
- Modify: `scripts/v5_position_manager.py`
- Modify: `tests/test_v5_position_manager.py`

**Interfaces:**
- Consumes: `_seeded_open_position(db_path) -> int` helper (existing);`MagicMock(spec=OkxTrader)`
- Produces:
  - `_update_closed` 新 param `exit_price_source`(default "monitor_tick")
  - `close_position` 成功/PERMANENT 分支填 source;成功分支还从 broker result 拿 fill price

## RED phase

- [ ] **Step 1: 追加 3 tests 到 `tests/test_v5_position_manager.py` 末尾**

```python


def test_close_success_uses_broker_fill_price(tmp_path):
    """F14: broker 返 {'success':True, 'price':0.99} → DB exit_price=0.99 (非 caller 0.163) + source=broker_fill。"""
    import sqlite3
    import json
    from unittest.mock import MagicMock
    from scripts.local_db import init_local_db
    from scripts.okx_trader import OkxTrader
    from scripts.v5_position_manager import V5PositionManager

    db = str(tmp_path / "x.db")
    init_local_db(db)
    pid = _seeded_open_position(db)

    mock_broker = MagicMock(spec=OkxTrader)
    mock_broker.close_position.return_value = {"success": True, "price": 0.99, "order_id": "rb"}
    pm = V5PositionManager(broker=mock_broker, db_path=db)
    pm.close_position(pid, exit_price=0.163, exit_reason="TP_HIT")

    conn = sqlite3.connect(db)
    row = conn.execute(
        "SELECT exit_price, error_context FROM positions_v5 WHERE id=?", (pid,)
    ).fetchone()
    conn.close()
    assert row[0] == 0.99, "应用 broker fill price 0.99, 非 caller 传入 0.163"
    ctx = json.loads(row[1])
    assert ctx["exit_price_source"] == "broker_fill"


def test_close_success_no_broker_price_falls_back_to_caller(tmp_path):
    """F14: broker 返 success 但无 price 字段 → fallback caller's exit_price + source=monitor_tick。"""
    import sqlite3
    import json
    from unittest.mock import MagicMock
    from scripts.local_db import init_local_db
    from scripts.okx_trader import OkxTrader
    from scripts.v5_position_manager import V5PositionManager

    db = str(tmp_path / "x.db")
    init_local_db(db)
    pid = _seeded_open_position(db)

    mock_broker = MagicMock(spec=OkxTrader)
    mock_broker.close_position.return_value = {"success": True, "order_id": "rb"}  # 无 price
    pm = V5PositionManager(broker=mock_broker, db_path=db)
    pm.close_position(pid, exit_price=0.163, exit_reason="TP_HIT")

    conn = sqlite3.connect(db)
    row = conn.execute(
        "SELECT exit_price, error_context FROM positions_v5 WHERE id=?", (pid,)
    ).fetchone()
    conn.close()
    assert row[0] == 0.163, "无 price → fallback caller"
    ctx = json.loads(row[1])
    assert ctx["exit_price_source"] == "monitor_tick"


def test_close_permanent_marks_monitor_tick_permanent_source(tmp_path):
    """F14: PERMANENT (交易所已平) → 用 caller exit_price + source=monitor_tick_permanent。"""
    import sqlite3
    import json
    from unittest.mock import MagicMock
    from scripts.local_db import init_local_db
    from scripts.okx_trader import OkxTrader
    from scripts.v5_position_manager import V5PositionManager

    db = str(tmp_path / "x.db")
    init_local_db(db)
    pid = _seeded_open_position(db)

    mock_broker = MagicMock(spec=OkxTrader)
    mock_broker.close_position.return_value = {
        "success": False, "error_kind": "PERMANENT",
        "error": "position not found on exchange",
    }
    pm = V5PositionManager(broker=mock_broker, db_path=db)
    pm.close_position(pid, exit_price=0.163, exit_reason="TP_HIT")

    conn = sqlite3.connect(db)
    row = conn.execute(
        "SELECT status, exit_price, error_context FROM positions_v5 WHERE id=?", (pid,)
    ).fetchone()
    conn.close()
    assert row[0] == "CLOSED"
    assert row[1] == 0.163
    ctx = json.loads(row[2])
    assert ctx["exit_price_source"] == "monitor_tick_permanent"
```

- [ ] **Step 2: 跑 tests —— 期望 RED**

```bash
python3 -m pytest tests/test_v5_position_manager.py::test_close_success_uses_broker_fill_price tests/test_v5_position_manager.py::test_close_success_no_broker_price_falls_back_to_caller tests/test_v5_position_manager.py::test_close_permanent_marks_monitor_tick_permanent_source -v
```

Expected:
- test 1 (broker_fill) FAIL:老代码用 caller 0.163 而非 broker 0.99;且 error_context 无 exit_price_source key → JSON parse OK 但 key error
- test 2 (fallback) FAIL:error_context 现是 None → `json.loads(None)` 抛 TypeError
- test 3 (permanent) FAIL:同上,error_context None + 无 source key

至少 3 test FAIL 就够 RED signal。

## GREEN phase

- [ ] **Step 3: `scripts/v5_position_manager.py` 修 `_update_closed` 签名 + 加 source 写 ctx**

用 Edit,anchor unique `def _update_closed(`.

**Before**(签名 + ctx 部分):
```python
    def _update_closed(
        self, conn: sqlite3.Connection, position_id: int, side: str,
        entry_price: float, size_usdt: float, leverage: int,
        entry_time_str: str, exit_price: float, exit_reason: str,
        existing_ctx_json: Optional[str],
    ) -> None:
        """成功平仓 or PERMANENT 补记账 —— 计算 PnL 并写 CLOSED。

        若 error_context 里有 close_error（前次 RETRYABLE 遗留），清掉。
        """
        entry_time = datetime.fromisoformat(entry_time_str)
        exit_time = _utcnow()
        holding_minutes = (exit_time - entry_time).total_seconds() / 60
        notional = (size_usdt or 0) * (leverage or 1)
        if side == "LONG":
            pnl_pct = (exit_price - entry_price) / entry_price
        else:
            pnl_pct = (entry_price - exit_price) / entry_price
        pnl_usdt = notional * pnl_pct

        # 清掉前次失败的 close_error（若有）,保留其他键（如 open_error）
        ctx = self._parse_error_context(existing_ctx_json)
        ctx.pop("close_error", None)
        # 空 dict 序列化为 "{}" 略啰嗦;若清完为空,存 NULL 更干净
        ctx_json = json.dumps(ctx) if ctx else None
```

**After**:
```python
    def _update_closed(
        self, conn: sqlite3.Connection, position_id: int, side: str,
        entry_price: float, size_usdt: float, leverage: int,
        entry_time_str: str, exit_price: float, exit_reason: str,
        existing_ctx_json: Optional[str],
        exit_price_source: str = "monitor_tick",
    ) -> None:
        """成功平仓 or PERMANENT 补记账 —— 计算 PnL 并写 CLOSED。

        若 error_context 里有 close_error（前次 RETRYABLE 遗留），清掉。
        F14: 加 exit_price_source 到 error_context 让审计可见 fill 来源。
        """
        entry_time = datetime.fromisoformat(entry_time_str)
        exit_time = _utcnow()
        holding_minutes = (exit_time - entry_time).total_seconds() / 60
        notional = (size_usdt or 0) * (leverage or 1)
        if side == "LONG":
            pnl_pct = (exit_price - entry_price) / entry_price
        else:
            pnl_pct = (entry_price - exit_price) / entry_price
        pnl_usdt = notional * pnl_pct

        # 清掉前次失败的 close_error（若有）,保留其他键（如 open_error）
        ctx = self._parse_error_context(existing_ctx_json)
        ctx.pop("close_error", None)
        ctx["exit_price_source"] = exit_price_source  # F14: audit trail
        ctx_json = json.dumps(ctx)  # ctx 至少含 source, 不再判空
```

(下面的 `conn.execute("""UPDATE positions_v5 ... error_context=?, ..."""` 行 + args 不变)

- [ ] **Step 4: 修 `close_position` 成功分支 —— 从 broker result 取 fill price + 传 source**

anchor unique(在 `if broker_error_kind is None:` 后)。

**Before**(约 L333-338):
```python
            if broker_error_kind is None:
                # 成功 → 正常 CLOSED
                self._update_closed(
                    conn, position_id, side, entry_price, size_usdt, leverage,
                    entry_time_str, exit_price, exit_reason, existing_ctx_json,
                )
```

**After**:
```python
            if broker_error_kind is None:
                # F14: broker 成功成交时优先用 fill price;无 price 字段 → fallback caller
                broker_price: Optional[float] = None
                if isinstance(rb_result, dict):
                    raw = rb_result.get("price")
                    if raw is not None:
                        try:
                            broker_price = float(raw)
                        except (TypeError, ValueError):
                            broker_price = None
                actual_exit = broker_price if broker_price is not None else exit_price
                source = "broker_fill" if broker_price is not None else "monitor_tick"
                self._update_closed(
                    conn, position_id, side, entry_price, size_usdt, leverage,
                    entry_time_str, actual_exit, exit_reason, existing_ctx_json,
                    exit_price_source=source,
                )
```

- [ ] **Step 5: 修 `close_position` PERMANENT 分支 —— 传 source="monitor_tick_permanent"**

anchor unique `elif broker_error_kind == "PERMANENT":`。

**Before**(约 L339-350):
```python
            elif broker_error_kind == "PERMANENT":
                # 交易所已平 → DB 补记账
                print(
                    f"[V5PositionManager] close broker PERMANENT: {broker_error_msg}; "
                    f"position {position_id} 交易所已平,DB 补记 CLOSED"
                )
                self._update_closed(
                    conn, position_id, side, entry_price, size_usdt, leverage,
                    entry_time_str, exit_price,
                    f"{exit_reason}|broker_permanent:{broker_error_msg}",
                    existing_ctx_json,
                )
```

**After**:
```python
            elif broker_error_kind == "PERMANENT":
                # 交易所已平 → DB 补记账
                print(
                    f"[V5PositionManager] close broker PERMANENT: {broker_error_msg}; "
                    f"position {position_id} 交易所已平,DB 补记 CLOSED"
                )
                # F14: 无 fill data → 保留 caller monitor tick 价 + 标 source
                self._update_closed(
                    conn, position_id, side, entry_price, size_usdt, leverage,
                    entry_time_str, exit_price,
                    f"{exit_reason}|broker_permanent:{broker_error_msg}",
                    existing_ctx_json,
                    exit_price_source="monitor_tick_permanent",
                )
```

- [ ] **Step 6: 跑 tests —— 期望 GREEN**

```bash
python3 -m pytest tests/test_v5_position_manager.py -v
```

Expected: 8/8 PASS(现有 5 + 新 3)

若失败:
- test 1 FAIL `exit_price != 0.99` → 检查 broker_price 提取 + actual_exit 选择
- test 1 FAIL error_context KeyError → 检查 ctx["exit_price_source"] 写入
- test 2 FAIL error_context None → 检查 ctx_json 是否改成 json.dumps(ctx) 无条件
- test 3 FAIL source ≠ monitor_tick_permanent → 检查 PERMANENT 分支是否传 source

## 邻近回归 + sanity + commit

- [ ] **Step 7: 邻近回归**

```bash
python3 -m pytest tests/test_v5_position_manager.py tests/test_v5_position_close.py -v 2>&1 | tail -20
```

Expected: 11/11 pass(8 + 3 F12 baseline)

**特别注意**:F12 的 `test_close_live_position_uses_broker` 现在 broker mock 返 `{"success": True}`(无 price)→ fallback → source=monitor_tick,行为一致,应 pass。

- [ ] **Step 8: sanity greps**

```bash
grep -c "exit_price_source" scripts/v5_position_manager.py
# 期望: ≥4 (1 param default + 1 ctx write + 2 calls with kwarg)

grep -c "broker_fill" scripts/v5_position_manager.py
# 期望: 1 (成功分支 source 三元)

grep -c "monitor_tick_permanent" scripts/v5_position_manager.py
# 期望: 1 (PERMANENT 分支 kwarg)

grep -c 'ctx_json = json.dumps(ctx)' scripts/v5_position_manager.py
# 期望: 1 (无条件 dumps, 不再判空)
```

- [ ] **Step 9: Commit**

```bash
git add scripts/v5_position_manager.py tests/test_v5_position_manager.py
git commit -m "$(cat <<'EOF'
fix(v5_position_manager): close 用 broker 实际成交价 + error_context 标 exit_price_source (Finding 14)

修 bug-fix-list.md Finding 14 (P1):V5PositionManager.close_position 用
caller (通常是 monitor tick) 的 exit_price 写 DB, 忽略 broker.close_position
返回值里的实际成交价 rb_result["price"]。滑点场景下 DB exit_price 与真实
成交价永久偏差, PnL 不准。

Change:
- _update_closed 加 exit_price_source: str = "monitor_tick" 参数
- ctx["exit_price_source"] 无条件写入 error_context (audit trail)
- ctx_json = json.dumps(ctx) 无条件 (从此永远非 NULL)
- close_position 成功分支: 从 rb_result["price"] 取 float, 有则用于 actual_exit
  + source="broker_fill"; 无则 fallback caller + source="monitor_tick"
- close_position PERMANENT 分支: 传 source="monitor_tick_permanent"
- 不改 monitor / paper / broker 内部 / PnL 公式 / 前端
- audit 建议的 fetch_my_trades 回填 YAGNI (标 source 已满足审计需求)

Tests:
- 追加 3 tests 到 test_v5_position_manager.py:
  - broker 返 price=0.99 → DB exit_price=0.99 + source=broker_fill
  - broker 无 price 字段 → fallback caller + source=monitor_tick
  - PERMANENT → caller price + source=monitor_tick_permanent
- 现有 5 tests 无回归 (老 mock 无 price 走 fallback, 结果不变)
- F12 邻近 test_v5_position_close.py 3/3 无回归

失效模式:
- error_context 从此永远非空; 若后端 audit 依赖 IS NULL 判断"无异常",
  需自行加 "过滤 exit_price_source 以外的 keys" 逻辑。前端目前无耦合。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

# Self-Review 记录

- **spec § 四 Change 1 (_update_closed 加 source)**: Step 3 ✓
- **spec § 五 Change 2 (成功分支 broker_price)**: Step 4 ✓
- **spec § 六 Change 3 (PERMANENT 分支 source)**: Step 5 ✓
- **spec § 七 Change 4 (3 tests)**: Step 1 ✓
- **spec § 八 验收**: Step 6 (8/8) + Step 7 (回归) + Step 8 (sanity) ✓
- **spec § 九 失效模式**: 已声明 broker price 非数值 fallback / PERMANENT 无 fill / error_context 从此非空 ✓
- **placeholder scan**: 无 TBD ✓
- **type consistency**:
  - `exit_price_source` 参数名 / 3 值(broker_fill / monitor_tick / monitor_tick_permanent)一致 ✓
- **测试 RED→GREEN**: Step 2 = RED (3 fail)；Step 6 = GREEN (8/8) ✓
- **atomicity**: 单 commit at Step 9 ✓
