# Bug Fix Batch 17 · Finding 17 · _resolve_leverage 静默改 WARN · Spec+Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `scripts/paper_position_manager.py:95-96` 的 `except Exception: pass` → `except Exception as e: print(WARN)`。与 F8 (Batch 7 get_param) 同款静默 catch 修复,DB 层异常时打日志,不再静默 fallback env/default 10。

**Architecture:** 单 task TDD:先写 1 test(RED — 现代码不 print WARN)→ 改 1 行 → GREEN → 单 commit。

**Tech Stack:** Python stdlib + pytest capsys/monkeypatch。无新增 pip 依赖。

## 问题陈述

`_resolve_leverage()` L82-96 尝试从 `exchange_config_manager` 拉 leverage(DB 优先);若 `get_exchange_config_manager()` 或 `mgr.get_config()` 抛 AttributeError / ImportError / sqlite Error,`except Exception: pass` 静默吞。函数回退 env → default 10。用户在 exchange config 页配的 leverage=3 若因 DB 表迁移中或 module bug 拉不到,paper trade 用 10× 计算 size,虚拟仓位比预期大 3.3×,KPI 失真。

## Global Constraints

- Only 2 files touched:
  - `scripts/paper_position_manager.py` (L95-96, 1 行改 2 行)
  - `tests/test_paper_position_manager_v5.py` (追加 1 test, 复用 `_fresh_db` helper)
- WARN 格式 EXACT: `f"[PaperPositionManager] _resolve_leverage DB 读取失败,回退到 env/default: {e}"`
- 保留 env / default fallback 行为(下面代码不动)
- 不改 supabase 分支 / env key 逻辑 / cache
- 不改其他函数
- 现有 tests 无回归:
  - `test_paper_position_manager_v5.py` 6 existing(F11 baseline)→ 7/7
- Single commit, subject EXACT: `fix(paper_position_manager): _resolve_leverage DB 错误改 WARN 日志 (Finding 17)`

## File Structure

| 路径 | 动作 |
|---|---|
| `scripts/paper_position_manager.py` | Modify L95-96 |
| `tests/test_paper_position_manager_v5.py` | Append 1 test |

---

# Task 1: 1 行改 + 1 test

**Files:**
- Modify: `scripts/paper_position_manager.py`
- Modify: `tests/test_paper_position_manager_v5.py`

**Interfaces:**
- Consumes: `capsys` + `monkeypatch` pytest fixtures
- Produces: 无新对外 API;`_resolve_leverage` 行为不变(仍返 env/default),但 DB 错时多 print 一行

## RED phase

- [ ] **Step 1: 追加 test 到 `tests/test_paper_position_manager_v5.py` 末尾**

```python


def test_resolve_leverage_db_error_logs_warning(monkeypatch, capsys):
    """F17: exchange_config_manager 抛异常 → print WARN + fallback env/default,不静默。"""
    from scripts.paper_position_manager import PaperPositionManager

    def _raise(_supabase):
        raise AttributeError("get_exchange_config_manager broken")

    monkeypatch.setattr(
        "scripts.exchange_config_manager.get_exchange_config_manager",
        _raise,
    )
    monkeypatch.delenv("OKX_LEVERAGE", raising=False)
    monkeypatch.delenv("BINANCE_LEVERAGE", raising=False)
    monkeypatch.delenv("EXCHANGE", raising=False)

    pm = PaperPositionManager(db_path=":memory:")
    result = pm._resolve_leverage()

    captured = capsys.readouterr()
    assert result == 10  # default fallback
    assert "[PaperPositionManager]" in captured.out
    assert "_resolve_leverage DB 读取失败" in captured.out
    assert "AttributeError" in captured.out or "broken" in captured.out
```

- [ ] **Step 2: 跑 test —— 期望 RED**

```bash
python3 -m pytest tests/test_paper_position_manager_v5.py::test_resolve_leverage_db_error_logs_warning -v
```

Expected: FAIL —— 老代码 `except Exception: pass` → captured.out 空 → `assert "[PaperPositionManager]" in captured.out` 失败。

## GREEN phase

- [ ] **Step 3: 改 `scripts/paper_position_manager.py:95-96`**

anchor unique(在 `_resolve_leverage` 函数内的两行):

**Before**:
```python
        except Exception:
            pass
```

**After**:
```python
        except Exception as e:
            print(f"[PaperPositionManager] _resolve_leverage DB 读取失败,回退到 env/default: {e}")
```

(不动上下文其他行:后面的 `env_active = ...` 段保持)

- [ ] **Step 4: 跑 test —— 期望 GREEN**

```bash
python3 -m pytest tests/test_paper_position_manager_v5.py -v
```

Expected: 7/7 PASS(6 F11 baseline + 1 新)

若失败:
- `[PaperPositionManager]` 不在 out → 检查 print 前缀
- `DB 读取失败` 不在 out → 检查中文字符 / f-string
- `result != 10` → 检查是否误改了后面的 env/default fallback 段

## 邻近回归 + sanity + commit

- [ ] **Step 5: 邻近回归**

```bash
python3 -m pytest tests/test_paper_position_manager_v5.py tests/test_v5_position_manager.py -v 2>&1 | tail -10
```

Expected:
- test_paper_position_manager_v5.py 7/7
- test_v5_position_manager.py 8/8 (F14 baseline)

- [ ] **Step 6: sanity greps**

```bash
grep -n "_resolve_leverage DB 读取失败" scripts/paper_position_manager.py
# 期望: 1 hit (L95 附近)

grep -n "except Exception:$" scripts/paper_position_manager.py
# 期望: 0 hits (L95 的老 pattern 已消失)
```

- [ ] **Step 7: Commit**

```bash
git add scripts/paper_position_manager.py tests/test_paper_position_manager_v5.py
git commit -m "$(cat <<'EOF'
fix(paper_position_manager): _resolve_leverage DB 错误改 WARN 日志 (Finding 17)

修 bug-fix-list.md Finding 17 (P2):_resolve_leverage 的 except Exception: pass
静默吞 exchange_config_manager 层异常(AttributeError / ImportError /
sqlite OperationalError)。DB 表迁移中若失败,函数回退 env/default=10,
用户 exchange config 页配的 leverage=3 静默失效,paper trade size 用 10x
计算,虚拟仓位比预期大 3.3x,KPI 失真。

Change:
- except Exception: pass → except Exception as e: print(WARN)
- 消息格式: [PaperPositionManager] _resolve_leverage DB 读取失败,回退到 env/default: {e}
- 保留 env / default fallback 行为
- 不改 supabase 分支 / env key 逻辑 / cache

Tests:
- 追加 test_resolve_leverage_db_error_logs_warning
- monkeypatch get_exchange_config_manager 抛 AttributeError + capsys 断言 WARN
- 邻近 test_v5_position_manager (F14) 无回归

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```
