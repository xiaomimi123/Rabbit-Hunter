# Bug Fix Batch 18 · Finding 18 · SettingsPage 同时活仓上限受控输入 · Spec+Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** SettingsPage.tsx:219 `<TextInput value="3" />` 硬编码无 onChange,用户修改无效。加受控 state + onBlur commit → `patch.mutate({ v5_max_concurrent: n })`。后端 schema + route + `system_settings` 写入配套。

**Architecture:** 后端 TDD 单 task(schema + route + 1 test);前端非 TDD(TS 类型保证 + 手动验证)。分 2 commits(后端 + 前端)但同一 batch 走 SDD 循环。

**Tech Stack:** Python pydantic + FastAPI + pytest;TypeScript + React + @tanstack/react-query。无新增 pip / npm 依赖。

## Global Constraints

- 4 files touched:
  - `api/schemas/v5_settings.py` (加 v5_max_concurrent 到 Response + Patch)
  - `api/routes/v5_settings.py` (get_settings 读 + patch_settings 写 + 1-10 validation)
  - `Rabbit Hunterfronted/types.ts` (加 v5_max_concurrent 到 SettingsResponse + SettingsPatchRequest)
  - `Rabbit Hunterfronted/components/pages-v4/SettingsPage.tsx` (受控 state + onBlur commit)
- 后端字段命名 EXACT:`v5_max_concurrent`(与 get_param key 一致)
- 后端 default:read 时若 system_settings 无该 key → return 3
- 后端 validation:patch 时若值 < 1 或 > 10 → HTTP 422
- 前端 state 从 `settings?.v5_max_concurrent ?? 3` 初始化,useEffect 在 settings 到达后 sync
- `TextInput` 组件加 `onBlur?: () => void` prop(不改现有 caller)
- 只对 L219 那一处 TextInput 加受控行为;其他 disabled TextInput 不动
- 不改 get_param 逻辑(read 侧 v5_params.get_param 已能读 system_settings)
- **不加前端 vitest UI test**(P2 scope 收敛;TS 类型 + 后端 test + 手动验证足够)
- 后端 tests 无回归:`test_v5_settings*` 若已存在,现有 pass 数不变;加 1 新 test
- 2 commits (后端 + 前端 分开):
  - `fix(v5_settings backend): 加 v5_max_concurrent read/write + validation (Finding 18 backend)`
  - `fix(SettingsPage): 同时活仓上限 TextInput 加受控 state + onBlur commit (Finding 18 frontend)`

---

## File Structure

| 路径 | 动作 |
|---|---|
| `api/schemas/v5_settings.py` | Modify — 2 字段加到 Response + Patch |
| `api/routes/v5_settings.py` | Modify — get 读 + patch 写 + validation |
| `Rabbit Hunterfronted/types.ts` | Modify — 2 字段加到 SettingsResponse + SettingsPatchRequest |
| `Rabbit Hunterfronted/components/pages-v4/SettingsPage.tsx` | Modify — state + onChange + onBlur(TextInput 组件 + Field 用法) |
| `tests/test_v5_settings_max_concurrent.py` | Create — 1 backend test |

---

# Task A: 后端 schema + route + test

**Files:**
- Modify: `api/schemas/v5_settings.py`
- Modify: `api/routes/v5_settings.py`
- Create: `tests/test_v5_settings_max_concurrent.py`

## RED phase (backend)

- [ ] **Step A1: 创建 `tests/test_v5_settings_max_concurrent.py`**

```python
"""Batch 18 Finding 18: SettingsPage 同时活仓上限 后端 API 契约测试。"""
import sqlite3
from fastapi.testclient import TestClient


def _init(db_path):
    conn = sqlite3.connect(db_path)
    conn.executescript('''
        CREATE TABLE system_settings (
            key TEXT PRIMARY KEY, value TEXT,
            created_at TEXT, updated_at TEXT
        );
    ''')
    conn.commit()
    conn.close()


def test_get_settings_default_v5_max_concurrent(monkeypatch, tmp_path):
    """system_settings 无 v5_max_concurrent → get_settings 返 default 3。"""
    db_path = tmp_path / "t.db"
    _init(str(db_path))
    monkeypatch.setenv("DB_PATH", str(db_path))
    from api.main import app
    client = TestClient(app)
    r = client.get("/api/v5/settings")
    assert r.status_code == 200
    assert r.json()["v5_max_concurrent"] == 3


def test_patch_v5_max_concurrent_writes_system_settings(monkeypatch, tmp_path):
    """PATCH v5_max_concurrent=5 → system_settings 有该 key,GET 返 5。"""
    db_path = tmp_path / "t.db"
    _init(str(db_path))
    monkeypatch.setenv("DB_PATH", str(db_path))
    from api.main import app
    client = TestClient(app)
    r = client.patch("/api/v5/settings", json={"v5_max_concurrent": 5})
    assert r.status_code == 200
    assert r.json()["v5_max_concurrent"] == 5

    conn = sqlite3.connect(str(db_path))
    row = conn.execute("SELECT value FROM system_settings WHERE key='v5_max_concurrent'").fetchone()
    conn.close()
    assert row is not None
    assert row[0] == "5"


def test_patch_v5_max_concurrent_rejects_out_of_range(monkeypatch, tmp_path):
    """PATCH v5_max_concurrent=100 → HTTP 422,未写入。"""
    db_path = tmp_path / "t.db"
    _init(str(db_path))
    monkeypatch.setenv("DB_PATH", str(db_path))
    from api.main import app
    client = TestClient(app)
    r = client.patch("/api/v5/settings", json={"v5_max_concurrent": 100})
    assert r.status_code == 422

    conn = sqlite3.connect(str(db_path))
    row = conn.execute("SELECT value FROM system_settings WHERE key='v5_max_concurrent'").fetchone()
    conn.close()
    assert row is None
```

- [ ] **Step A2: 跑 tests —— 期望 RED**

```bash
python3 -m pytest tests/test_v5_settings_max_concurrent.py -v
```

Expected: 3/3 FAIL (老 SettingsResponse 无 `v5_max_concurrent` 字段 → KeyError / 422 unrecognized field / pydantic 拒生成)

## GREEN phase (backend)

- [ ] **Step A3: 修 `api/schemas/v5_settings.py`**

anchor unique `class SettingsResponse(BaseModel):`.

**Before**:
```python
class SettingsResponse(BaseModel):
    exchange: Literal["okx", "binance"]
    ...
    sl_tp_fail_open: bool
```

**After**(在 `sl_tp_fail_open` 后追加):
```python
class SettingsResponse(BaseModel):
    exchange: Literal["okx", "binance"]
    ...
    sl_tp_fail_open: bool
    v5_max_concurrent: int = 3
```

anchor `class SettingsPatchRequest(BaseModel):`.

**Before**:
```python
class SettingsPatchRequest(BaseModel):
    """全部字段可选。..."""
    exchange: Optional[Literal["okx", "binance"]] = None
    ...
    sl_tp_fail_open: Optional[bool] = None
```

**After**(在 `sl_tp_fail_open` 后追加,pydantic Field 加 range validation):
```python
from pydantic import BaseModel, Field  # 若 Field 尚未 import

class SettingsPatchRequest(BaseModel):
    """全部字段可选。..."""
    exchange: Optional[Literal["okx", "binance"]] = None
    ...
    sl_tp_fail_open: Optional[bool] = None
    v5_max_concurrent: Optional[int] = Field(default=None, ge=1, le=10)
```

**注意**:确认 `from pydantic import BaseModel` 上方或改成 `from pydantic import BaseModel, Field`。

- [ ] **Step A4: 修 `api/routes/v5_settings.py:55-107` get_settings + patch_settings**

**Change 1**:`get_settings()` finally 之前(约 L92 前)加一行读:
```python
        v5_max_concurrent_raw = _read_setting(conn, "v5_max_concurrent")
        try:
            v5_max_concurrent = int(v5_max_concurrent_raw) if v5_max_concurrent_raw else 3
        except ValueError:
            v5_max_concurrent = 3
```

Return 处加字段:
```python
    return SettingsResponse(
        ...,
        sl_tp_fail_open=sl_tp_fail_open,
        v5_max_concurrent=v5_max_concurrent,
    )
```

**Change 2**:`patch_settings()` L254 后加:
```python
        if req.v5_max_concurrent is not None:
            _write_setting(conn, "v5_max_concurrent", str(req.v5_max_concurrent))
```

- [ ] **Step A5: 跑 tests —— 期望 GREEN**

```bash
python3 -m pytest tests/test_v5_settings_max_concurrent.py -v
```

Expected: 3/3 PASS

## 后端 sanity + commit

- [ ] **Step A6: sanity**

```bash
grep -c "v5_max_concurrent" api/schemas/v5_settings.py
# 期望: 2 (Response + Patch)

grep -c "v5_max_concurrent" api/routes/v5_settings.py
# 期望: ≥3 (read + return + patch write)
```

- [ ] **Step A7: 后端 Commit**

```bash
git add api/schemas/v5_settings.py api/routes/v5_settings.py tests/test_v5_settings_max_concurrent.py
git commit -m "$(cat <<'EOF'
fix(v5_settings backend): 加 v5_max_concurrent read/write + validation (Finding 18 backend)

修 bug-fix-list.md Finding 18 (P2) 后端部分:SettingsResponse / SettingsPatchRequest
不含 v5_max_concurrent,PATCH 也不写 system_settings 该 key。前端 SettingsPage
即便加 onChange 也无法真正保存。

Change:
- SettingsResponse 加 v5_max_concurrent: int = 3
- SettingsPatchRequest 加 v5_max_concurrent: Optional[int] = Field(ge=1, le=10)
- get_settings 从 system_settings 读, 缺失或非法返 3
- patch_settings 写 str(int) 到 system_settings v5_max_concurrent
- v5_params.get_param 已能从 system_settings 读该 key, 无需改

Tests:
- 新增 tests/test_v5_settings_max_concurrent.py 3 tests:
  - GET default = 3
  - PATCH=5 → GET=5 + DB 有 key
  - PATCH=100 → 422 + DB 无 key

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

# Task B: 前端 types + SettingsPage 受控输入

**Files:**
- Modify: `Rabbit Hunterfronted/types.ts`
- Modify: `Rabbit Hunterfronted/components/pages-v4/SettingsPage.tsx`

## Steps (无 TDD, 依赖 TS 类型 + 后端 test + 手动验证)

- [ ] **Step B1: 修 `Rabbit Hunterfronted/types.ts`**

anchor unique `export interface SettingsResponse {`.

在 `sl_tp_fail_open: boolean;` 后加:
```ts
  v5_max_concurrent: number;
```

anchor unique `export interface SettingsPatchRequest {`.

在 `sl_tp_fail_open?: boolean;` 后加:
```ts
  v5_max_concurrent?: number;
```

- [ ] **Step B2: 修 `Rabbit Hunterfronted/components/pages-v4/SettingsPage.tsx` TextInput 组件加 onBlur**

anchor unique `function TextInput({ value, type = 'text', onChange, placeholder, disabled }: {`.

**Before**:
```tsx
function TextInput({ value, type = 'text', onChange, placeholder, disabled }: {
  value: string | number; type?: 'text' | 'password';
  onChange?: (v: string) => void; placeholder?: string; disabled?: boolean;
}) {
  return (
    <input
      type={type}
      value={value}
      onChange={(e) => onChange?.(e.target.value)}
      placeholder={placeholder}
      disabled={disabled}
      className="..."
    />
  );
}
```

**After**:
```tsx
function TextInput({ value, type = 'text', onChange, onBlur, placeholder, disabled }: {
  value: string | number; type?: 'text' | 'password';
  onChange?: (v: string) => void; onBlur?: () => void;
  placeholder?: string; disabled?: boolean;
}) {
  return (
    <input
      type={type}
      value={value}
      onChange={(e) => onChange?.(e.target.value)}
      onBlur={onBlur}
      placeholder={placeholder}
      disabled={disabled}
      className="..."
    />
  );
}
```

- [ ] **Step B3: 修 SettingsPage 顶部加 state + effect**

在 `import { useState } from 'react';` 附近改为 `import { useEffect, useState } from 'react';`(若 useEffect 未 import)。

在 SettingsPage 组件函数顶部(如 `const { query, patch, testAi } = useV5Settings();` 之后)加:
```tsx
  const settings = query.data;
  const [maxConcurrentDraft, setMaxConcurrentDraft] = useState<string>('');
  useEffect(() => {
    if (settings?.v5_max_concurrent != null) {
      setMaxConcurrentDraft(String(settings.v5_max_concurrent));
    }
  }, [settings?.v5_max_concurrent]);
  const commitMaxConcurrent = () => {
    const n = Number(maxConcurrentDraft);
    if (Number.isInteger(n) && n >= 1 && n <= 10 && n !== settings?.v5_max_concurrent) {
      patch.mutate({ v5_max_concurrent: n } as any);
    }
  };
```

**注意**:若 `settings` 或 `query.data` 已在文件里被其他方式 destructure,复用现有变量,不要重复定义。

- [ ] **Step B4: 修 L219 那个 TextInput**

**Before**:
```tsx
            <Field title="同时活仓上限" hint="v5_max_concurrent"
              control={<TextInput value="3" />} />
```

**After**:
```tsx
            <Field title="同时活仓上限" hint="v5_max_concurrent · 范围 1-10"
              control={<TextInput
                value={maxConcurrentDraft}
                onChange={setMaxConcurrentDraft}
                onBlur={commitMaxConcurrent}
              />} />
```

- [ ] **Step B5: TypeScript 检查 + build 试跑**

```bash
cd "Rabbit Hunterfronted" && npx tsc --noEmit 2>&1 | tail -10
```

Expected: 无新 TS error(现有 error 若有则 unchanged baseline)

若失败:
- 'v5_max_concurrent' does not exist on type → 检查 types.ts 是否真加了字段
- Type 'unknown' is not assignable → 检查 `as any` cast 是否加了

- [ ] **Step B6: 邻近前端 tests 无回归**

```bash
cd "Rabbit Hunterfronted" && npm test -- tests/hooks/ 2>&1 | tail -15
```

Expected: 13/13 pass(F15/F16 baseline)

- [ ] **Step B7: 前端 Commit**

```bash
git add "Rabbit Hunterfronted/types.ts" "Rabbit Hunterfronted/components/pages-v4/SettingsPage.tsx"
git commit -m "$(cat <<'EOF'
fix(SettingsPage): 同时活仓上限 TextInput 加受控 state + onBlur commit (Finding 18 frontend)

修 bug-fix-list.md Finding 18 (P2) 前端部分:SettingsPage.tsx:219 硬编码
<TextInput value="3" /> 无 onChange, 用户输入完全无效, 运营以为改了
但后端 v5_max_concurrent 永远不变。

Change:
- types.ts SettingsResponse 加 v5_max_concurrent: number
- types.ts SettingsPatchRequest 加 v5_max_concurrent?: number
- TextInput 组件加 onBlur?: () => void prop(其他 caller 不变)
- SettingsPage 加 maxConcurrentDraft state, useEffect 从 settings sync
- L219 TextInput 改为受控: value + onChange + onBlur commit
- onBlur 时校验 (integer, 1-10, 与当前不同) 才 patch.mutate

不加前端 UI vitest test (P2 scope 收敛; TS 类型 + 后端 test + 手动验证足够)

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

# Self-Review

- 后端: schema + route + 3 tests 全面覆盖
- 前端: TS 类型链路完整,onBlur commit 有校验
- 分 2 commits 让后端可独立 rollback / 前端可等 API 部署后再发
