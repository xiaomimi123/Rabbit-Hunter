# Bug Fix Batch 11 · Finding 11 · open_position BEGIN IMMEDIATE 二次 count · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `PaperPositionManager.open_position()` 内 BEGIN IMMEDIATE 事务内二次 count paper+live OPEN,若超 `max_concurrent` 抛 `ConcurrencyLimitExceeded`。scorer catch 后写 `MAX_CONCURRENT_POSITIONS_RACE` block reason。

**Architecture:** 单 task TDD:先写 2 tests(RED —— 无 `max_concurrent` 参数 → TypeError,无异常类 → ImportError)→ 加异常类 + 参数 + BEGIN IMMEDIATE → GREEN → 单 commit。

**Tech Stack:** Python stdlib + pytest tmp_path + sqlite3。无新增 pip 依赖。

## Global Constraints

- Only 3 files touched:
  - `scripts/paper_position_manager.py` (加异常类 + 修 open_position)
  - `scripts/tasks/scorer.py` (import + SHADOW 分支传参 + catch)
  - `tests/test_paper_position_manager_v5.py` (追加 2 tests)
- 异常类名 EXACT: `ConcurrencyLimitExceeded`(定义在 `scripts/paper_position_manager.py` 顶层)
- 参数名 EXACT: `max_concurrent: Optional[int] = None`
- SQL 关键:`BEGIN IMMEDIATE` + count 双表(paper_trades OPEN + positions_v5 OPEN,后者 try/except 兜 OperationalError)
- 抛异常 msg 需含 "并发上限已达"
- scorer 写 block_reason EXACT: `MAX_CONCURRENT_POSITIONS_RACE`
- LIVE 路径(V5PositionManager.open_position)不改
- 现有 tests 无回归:
  - `test_paper_position_manager_v5.py` 现 4 tests → 6/6 (加 2)
  - `test_v5_scorer.py`、`test_v5_scorer_run_catches.py` 无回归
- 现有 helpers 复用:`_fresh_db()` + `_open_intent_kwargs()`
- Single commit, subject EXACT: `fix(paper_position_manager): open_position BEGIN IMMEDIATE + 二次 count 校验 (Finding 11)`

---

## File Structure

| 路径 | 动作 |
|---|---|
| `scripts/paper_position_manager.py` | Modify —— 加异常类 + open_position 加参数/BEGIN IMMEDIATE |
| `scripts/tasks/scorer.py` | Modify —— import 异常 + 传参 + 前置 catch |
| `tests/test_paper_position_manager_v5.py` | Modify —— 追加 2 tests |

---

# Task 1: 全套修改 + 2 tests

**Files:**
- Modify: `scripts/paper_position_manager.py`
- Modify: `scripts/tasks/scorer.py`
- Modify: `tests/test_paper_position_manager_v5.py`

**Interfaces:**
- Consumes: 现有 `_fresh_db()` / `_open_intent_kwargs()` helpers
- Produces:
  - 新导出 `ConcurrencyLimitExceeded` 异常类
  - `open_position` 加 `max_concurrent: Optional[int] = None` kwarg
  - scorer 新 block reason: `MAX_CONCURRENT_POSITIONS_RACE`

## RED phase

- [ ] **Step 1: 追加 2 tests 到 `tests/test_paper_position_manager_v5.py` 末尾**

复用现有的 `_fresh_db()` + `_open_intent_kwargs()`。追加:

```python


def test_open_position_race_double_check_rejects_when_at_limit():
    """Finding 11: max_concurrent=3, 已有 3 OPEN paper → 二次校验抛 ConcurrencyLimitExceeded, 无 INSERT。"""
    from scripts.paper_position_manager import (
        PaperPositionManager, ConcurrencyLimitExceeded,
    )
    db = _fresh_db()
    conn = sqlite3.connect(db)
    for _ in range(3):
        conn.execute(
            "INSERT INTO paper_trades (symbol, side, status, strategy_id, "
            "created_at, entry_time) "
            "VALUES ('BTC/USDT', 'LONG', 'OPEN', 'v5', datetime('now'), datetime('now'))"
        )
    conn.commit()
    conn.close()

    pm = PaperPositionManager(db_path=db)
    with pytest.raises(ConcurrencyLimitExceeded, match="并发上限已达"):
        pm.open_position(**_open_intent_kwargs(), max_concurrent=3)

    conn = sqlite3.connect(db)
    n = conn.execute(
        "SELECT COUNT(*) FROM paper_trades WHERE status='OPEN'"
    ).fetchone()[0]
    conn.close()
    assert n == 3, "上限达到时不应再 INSERT"


def test_open_position_no_max_concurrent_bypasses_check():
    """Finding 11: max_concurrent=None(默认)→ 不校验,即便 100 条 OPEN 也放行。"""
    from scripts.paper_position_manager import PaperPositionManager
    db = _fresh_db()
    conn = sqlite3.connect(db)
    for _ in range(100):
        conn.execute(
            "INSERT INTO paper_trades (symbol, side, status, strategy_id, "
            "created_at, entry_time) "
            "VALUES ('BTC/USDT', 'LONG', 'OPEN', 'v5', datetime('now'), datetime('now'))"
        )
    conn.commit()
    conn.close()

    pm = PaperPositionManager(db_path=db)
    pid = pm.open_position(**_open_intent_kwargs())  # 无 max_concurrent
    assert pid > 0
```

- [ ] **Step 2: 跑 tests —— 期望 RED**

```bash
python3 -m pytest tests/test_paper_position_manager_v5.py::test_open_position_race_double_check_rejects_when_at_limit tests/test_paper_position_manager_v5.py::test_open_position_no_max_concurrent_bypasses_check -v
```

Expected:
- test 1 FAIL/ERROR:`ImportError: cannot import name 'ConcurrencyLimitExceeded'` 或 `TypeError: open_position() got an unexpected keyword argument 'max_concurrent'`
- test 2 PASS(现有 open_position 无参数校验,能 INSERT)—— 但会不小心 pass,能接受

如果 test 2 意外 PASS 说明 no-param 路径不变,验证 backward-compat。至少 test 1 FAIL 就够 RED signal。

## GREEN phase

- [ ] **Step 3: `scripts/paper_position_manager.py` 顶层加异常类**

用 Edit 定位:文件顶部 import 完但 `class PaperPositionManager:` 之前的位置。unique anchor 是 `class PaperPositionManager:`(在插入点之后)。

**Before**(约 L47):
```python
class PaperPositionManager:
    """V5 虚拟仓位管理器。
```

**After**:
```python
class ConcurrencyLimitExceeded(Exception):
    """开仓时检测到当前 OPEN 数已达 max_concurrent 上限(TOCTOU 二次校验)。

    Finding 11: BEGIN IMMEDIATE 事务内二次 count 双表(paper_trades + positions_v5),
    上层 catch 后应写 block_reason=MAX_CONCURRENT_POSITIONS_RACE,不重试。
    """


class PaperPositionManager:
    """V5 虚拟仓位管理器。
```

- [ ] **Step 4: 修 `PaperPositionManager.open_position()` 签名 + 事务**

用 Edit 定位:方法头 `def open_position(self, *, enriched, indicators, decision, risk, ai) -> int:`。

**Before**(签名一行):
```python
    def open_position(self, *, enriched, indicators, decision, risk, ai) -> int:
```

**After**:
```python
    def open_position(self, *, enriched, indicators, decision, risk, ai,
                      max_concurrent: Optional[int] = None) -> int:
```

然后在 `conn = self._conn()` 后、`try:` 里的 `cur = conn.execute("""INSERT INTO paper_trades ...` 之前,插入 BEGIN IMMEDIATE + 二次校验块。

定位 anchor(unique):
```python
        conn = self._conn()
        try:
            cur = conn.execute("""
                INSERT INTO paper_trades (
```

**Before**:
```python
        conn = self._conn()
        try:
            cur = conn.execute("""
                INSERT INTO paper_trades (
```

**After**:
```python
        conn = self._conn()
        try:
            if max_concurrent is not None:
                # Finding 11: BEGIN IMMEDIATE + 二次 count paper + live
                conn.execute("BEGIN IMMEDIATE")
                n_paper = conn.execute(
                    "SELECT COUNT(*) FROM paper_trades WHERE status='OPEN'"
                ).fetchone()[0]
                try:
                    n_live = conn.execute(
                        "SELECT COUNT(*) FROM positions_v5 WHERE status='OPEN'"
                    ).fetchone()[0]
                except sqlite3.OperationalError:
                    n_live = 0  # positions_v5 表可能未 mig,视为 0
                if n_paper + n_live >= max_concurrent:
                    conn.rollback()
                    raise ConcurrencyLimitExceeded(
                        f"open_position 并发上限已达:{n_paper}(paper)+{n_live}(live)"
                        f">={max_concurrent},拒绝开仓"
                    )
            cur = conn.execute("""
                INSERT INTO paper_trades (
```

- [ ] **Step 5: 修 `scripts/tasks/scorer.py` —— import + 传参 + catch**

先加 import(在文件顶部现有 imports 附近):

用 Edit,anchor unique:一处现有 import(如 `from scripts.paper_position_manager import`)。若没有该 import,在 `import ...` 段尾加:

```python
from scripts.paper_position_manager import ConcurrencyLimitExceeded
```

**注意**:如果找不到合适 anchor,可以在 `scripts/tasks/scorer.py` 顶层 import 区靠前(比如 `import sqlite3` 之后)插入。

然后修改 SHADOW 分支的 open_position 调用 + catch 结构:

**Before**(约 L408-427):
```python
    try:
        if mode == "SHADOW":
            position_id = paper_pm.open_position(
                enriched=enriched, indicators=indicators,
                decision=decision, risk=risk, ai=ai_result,
            )
        else:
            position_id = live_pm.open_position(
                symbol=enriched.symbol, side=decision.side,
                entry_price=risk.entry_price, sl_price=risk.sl_price,
                tp_price=risk.tp_price, size_usdt=risk.size_usdt,
                leverage=risk.leverage,
            )
    except Exception as e:
        _write_trade_score(db_path, enriched, indicators, decision,
                          ai=ai_result, risk=risk,
                          block_reason=f"OPEN_FAILED:{type(e).__name__}",
                          funding_z_score=funding_z_score,
                          funding_rate_8h=funding_rate_8h)
        return
```

**After**:
```python
    try:
        if mode == "SHADOW":
            position_id = paper_pm.open_position(
                enriched=enriched, indicators=indicators,
                decision=decision, risk=risk, ai=ai_result,
                max_concurrent=_max_concurrent(),
            )
        else:
            position_id = live_pm.open_position(
                symbol=enriched.symbol, side=decision.side,
                entry_price=risk.entry_price, sl_price=risk.sl_price,
                tp_price=risk.tp_price, size_usdt=risk.size_usdt,
                leverage=risk.leverage,
            )
    except ConcurrencyLimitExceeded as e:
        _write_trade_score(db_path, enriched, indicators, decision,
                          ai=ai_result, risk=risk,
                          block_reason="MAX_CONCURRENT_POSITIONS_RACE",
                          funding_z_score=funding_z_score,
                          funding_rate_8h=funding_rate_8h)
        print(f"[V5Scorer] {enriched.symbol} 并发二次校验拒开: {e}")
        return
    except Exception as e:
        _write_trade_score(db_path, enriched, indicators, decision,
                          ai=ai_result, risk=risk,
                          block_reason=f"OPEN_FAILED:{type(e).__name__}",
                          funding_z_score=funding_z_score,
                          funding_rate_8h=funding_rate_8h)
        return
```

- [ ] **Step 6: 跑 tests —— 期望 GREEN**

```bash
python3 -m pytest tests/test_paper_position_manager_v5.py -v
```

Expected: 6/6 PASS(现有 4 + 新 2)。

若失败:
- test 1 `pytest.raises` 不抛 → 检查 BEGIN IMMEDIATE 分支是否真被执行(max_concurrent 是否为 None-check 正确)
- test 1 抛了但 msg 不含 "并发上限已达" → 检查 raise msg 中文字符
- test 1 rows 数变 4 → 检查 INSERT 前 rollback 是否有效
- test 2 fail → 检查默认参数值确实是 None

## 邻近回归 + sanity + commit

- [ ] **Step 7: 邻近回归**

```bash
python3 -m pytest tests/test_paper_position_manager_v5.py tests/test_v5_scorer.py tests/test_v5_scorer_run_catches.py -v 2>&1 | tail -20
```

Expected:
- `test_paper_position_manager_v5.py` 6/6 PASS
- `test_v5_scorer.py` 现 baseline 保持(1/1 或已有值)
- `test_v5_scorer_run_catches.py` 现 baseline 保持(1/1)

若 scorer 相关 tests fail:检查 import 是否引入了循环 / 新 catch 前置是否吞了其他异常。

- [ ] **Step 8: sanity greps**

```bash
grep -c "ConcurrencyLimitExceeded" scripts/paper_position_manager.py
# 期望: 2(定义 class + raise)

grep -c "ConcurrencyLimitExceeded" scripts/tasks/scorer.py
# 期望: 2(import + except)

grep -c "BEGIN IMMEDIATE" scripts/paper_position_manager.py
# 期望: 1

grep -c "MAX_CONCURRENT_POSITIONS_RACE" scripts/tasks/scorer.py
# 期望: 1

# 老 pre-check 仍在(不 regress F3/F11 早退优化)
grep -c "MAX_CONCURRENT_POSITIONS" scripts/tasks/scorer.py
# 期望: 2 (老 pre-check 的 block_reason + 新 _RACE)
```

- [ ] **Step 9: Commit**

```bash
git add scripts/paper_position_manager.py scripts/tasks/scorer.py tests/test_paper_position_manager_v5.py
git commit -m "$(cat <<'EOF'
fix(paper_position_manager): open_position BEGIN IMMEDIATE + 二次 count 校验 (Finding 11)

修 bug-fix-list.md Finding 11 (P1):scorer 在 _count_open_positions 预检
后到 paper_pm.open_position 之间隔 await ai.decide(),count 可能失效。
未来多 scorer 实例时,两实例都读到 count 未满,各自 INSERT → 超上限。

Change:
- scripts/paper_position_manager.py 新增 ConcurrencyLimitExceeded 异常
- PaperPositionManager.open_position() 加 max_concurrent kwarg(默认 None)
- 传入时,BEGIN IMMEDIATE 内 count paper_trades OPEN + positions_v5 OPEN
  (positions_v5 缺表 try/except 兜为 0)。>= 上限则 rollback + raise
- scripts/tasks/scorer.py SHADOW 分支传 max_concurrent=_max_concurrent()
- catch ConcurrencyLimitExceeded 前置,写 MAX_CONCURRENT_POSITIONS_RACE
- LIVE 路径不改(V5PositionManager.open_position 走 broker,DB 锁跨 broker
  意义不大,单 scorer 场景 pre-check 足够,多 scorer LIVE 是后续 batch)

Tests:
- 新增 2 tests 到 test_paper_position_manager_v5.py
  - 已有 3 OPEN + max_concurrent=3 → 抛 + rows 保持 3
  - max_concurrent=None(默认)→ 不校验,100 OPEN 仍放行
- 现有 4 tests + scorer 邻近 tests 无回归

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

# Self-Review 记录

- **spec § 四 Change 1 (异常类)**: Step 3 ✓
- **spec § 五 Change 2 (open_position 签名 + BEGIN IMMEDIATE)**: Step 4 ✓
- **spec § 六 Change 3 (scorer 传参 + catch)**: Step 5 ✓
- **spec § 七 Change 4 (2 tests)**: Step 1 ✓
- **spec § 八 验收**: Step 6 (6/6) + Step 7 (邻近回归) + Step 8 (sanity) ✓
- **spec § 九 失效模式**: 已声明 positions_v5 缺表 + LIVE 未加锁 + max_concurrent=0 fail-fast ✓
- **placeholder scan**: 无 TBD ✓
- **type consistency**:
  - 异常类名 `ConcurrencyLimitExceeded` 在 spec + plan + test + scorer 一致 ✓
  - 参数名 `max_concurrent` 一致 ✓
  - block reason `MAX_CONCURRENT_POSITIONS_RACE` 一致 ✓
- **测试 RED→GREEN**: Step 2 = RED (test 1 fail)；Step 6 = GREEN (6/6) ✓
- **atomicity**: 单 commit at Step 9 ✓
