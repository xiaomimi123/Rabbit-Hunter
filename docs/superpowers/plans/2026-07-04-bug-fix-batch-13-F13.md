# Bug Fix Batch 13 · Finding 13 · manual-order execute 走 M3 铁律 · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `POST /api/v5/manual-order/execute` 在调 `pm.open_position()` 之前顺序跑 4 层 M3 gate(short_disabled / setup_enabled / daily_drawdown / per_trade_risk),任一违规 → HTTP 400 + JSON detail `{error_kind, gate, message}`,不开仓。

**Architecture:** 单 task TDD:先写 3 tests(RED — 老代码 SHORT 直接过, 无 gate)→ 加 4 层 gate + Response 结构 → GREEN → 单 commit。

**Tech Stack:** Python stdlib + FastAPI TestClient + pytest monkeypatch + unittest.mock。无新增 pip 依赖。

## Global Constraints

- Only 2 files touched:
  - `api/routes/v5_manual_order.py` (加 imports + 4 层 gate 在 `pm.open_position` 之前)
  - `tests/test_v5_manual_order_api.py` (改现有 test 1 + 追加 2 tests)
- 4 层 gate 顺序 EXACT:short_disabled → setup_enabled → daily_drawdown → per_trade_risk
- 每层 gate 违规都 raise `HTTPException(status_code=400, detail={"error_kind":..., "gate":..., "message":...})`
- `get_config(reload=True)` —— reload 每次(避免测试间全局 singleton 缓存污染)
- `get_today_realized_pnl` from `scripts.risk_gates`(核实过位置)
- 设 `setup_type` 时 funding_z_score=None(manual 不查 funding,与 scorer 无 funding 场景等价)
- Response 成功路径不改(仍是 ManualOrderExecuteResponse)
- 不改 preview 端点 / _build_full_context 内部 / 前端 / gate_* 内部 / config 读取来源
- 现有 `test_preview_returns_full_snapshot` 无回归
- Single commit, subject EXACT: `fix(v5_manual_order): execute 端点走 M3 铁律 4 层守卫 (Finding 13)`

---

## File Structure

| 路径 | 动作 |
|---|---|
| `api/routes/v5_manual_order.py` | Modify — imports + 4 层 gate 序列 |
| `tests/test_v5_manual_order_api.py` | Modify — fix test 1 + 追加 2 tests |

---

# Task 1: 4 层 M3 gate + 3 tests

**Files:**
- Modify: `api/routes/v5_manual_order.py`
- Modify: `tests/test_v5_manual_order_api.py`

**Interfaces:**
- Consumes: `scripts.risk_gates`,`scripts.config.get_config`,`scripts.ai.setup_type.derive_setup_type`,`scripts.v5_params.get_param`
- Produces: 4 层拒单路径,violation 走 HTTPException 400 detail

## RED phase

- [ ] **Step 1: 修 `tests/test_v5_manual_order_api.py`:更新现有 test 1 + 追加 2 tests**

**1a. 修 `test_execute_writes_paper_trade`**:在 `monkeypatch.setenv("V5_RSI_OVERBOUGHT", "60")` 后一行加:

```python
    monkeypatch.setenv("ENABLE_SHORT_TRADING", "true")
    from scripts.config import get_config
    get_config(reload=True)  # 清 singleton 让 env 生效
```

(在 `with patch(...) as mock_ta_cls:` 之前)

**1b. 在文件末尾追加 2 tests**:

```python


def test_execute_blocks_short_when_disabled(app_with_db, monkeypatch):
    """SHORT + enable_short_trading=false → 400 SHORT_DISABLED, 无 INSERT。"""
    klines_15m, klines_4h = _fake_klines()
    _inject_fake_exchange(monkeypatch, klines_15m, klines_4h)
    monkeypatch.setenv("V5_RSI_OVERBOUGHT", "60")
    monkeypatch.delenv("ENABLE_SHORT_TRADING", raising=False)
    from scripts.config import get_config
    get_config(reload=True)

    from v5_types import AIResult
    with patch("scripts.ai.trading_assistant.TradingAssistant") as mock_ta_cls:
        mock_ta = mock_ta_cls.return_value
        mock_ta.client = object()
        mock_ta.provider = "deepseek"
        mock_ta.assistant_id = None
        mock_ta.decide = AsyncMock(return_value=AIResult(
            execute=True, sl_multiplier=1.0, tp_multiplier=1.0,
            size_multiplier=1.0, confidence=0.7, reasoning="ok"))

        client, db = app_with_db
        r = client.post("/api/v5/manual-order/execute", json={
            "symbol": "TEST/USDT", "side": "SHORT", "size_usdt": 15.0,
            "sl_multiplier": 1.0, "tp_multiplier": 1.0, "size_multiplier": 1.0,
        })
        assert r.status_code == 400, r.text
        body = r.json()
        assert body["detail"]["error_kind"] == "SHORT_DISABLED"
        assert body["detail"]["gate"] == "enable_short_trading"

    conn = sqlite3.connect(db)
    n = conn.execute("SELECT COUNT(*) FROM paper_trades WHERE status='OPEN'").fetchone()[0]
    conn.close()
    assert n == 0, "违规 SHORT 不应产生 OPEN 记录"


def test_execute_blocks_when_daily_drawdown_exceeded(app_with_db, monkeypatch):
    """monkeypatch gate_daily_drawdown 抛 IronlawViolation → 400 DAILY_DRAWDOWN_HIT。"""
    klines_15m, klines_4h = _fake_klines()
    _inject_fake_exchange(monkeypatch, klines_15m, klines_4h)
    monkeypatch.setenv("V5_RSI_OVERBOUGHT", "60")
    monkeypatch.setenv("ENABLE_SHORT_TRADING", "true")
    from scripts.config import get_config
    get_config(reload=True)

    from scripts.risk_gates import IronlawViolation

    def _bang(**_):
        raise IronlawViolation("DAILY_DRAWDOWN_HIT", "day pnl -50 exceeds cap 3%")

    monkeypatch.setattr("api.routes.v5_manual_order.gate_daily_drawdown", _bang)

    from v5_types import AIResult
    with patch("scripts.ai.trading_assistant.TradingAssistant") as mock_ta_cls:
        mock_ta = mock_ta_cls.return_value
        mock_ta.client = object()
        mock_ta.provider = "deepseek"
        mock_ta.assistant_id = None
        mock_ta.decide = AsyncMock(return_value=AIResult(
            execute=True, sl_multiplier=1.0, tp_multiplier=1.0,
            size_multiplier=1.0, confidence=0.7, reasoning="ok"))

        client, db = app_with_db
        r = client.post("/api/v5/manual-order/execute", json={
            "symbol": "TEST/USDT", "side": "SHORT", "size_usdt": 15.0,
            "sl_multiplier": 1.0, "tp_multiplier": 1.0, "size_multiplier": 1.0,
        })
        assert r.status_code == 400, r.text
        body = r.json()
        assert body["detail"]["error_kind"] == "DAILY_DRAWDOWN_HIT"
        assert body["detail"]["gate"] == "gate_daily_drawdown"

    conn = sqlite3.connect(db)
    n = conn.execute("SELECT COUNT(*) FROM paper_trades WHERE status='OPEN'").fetchone()[0]
    conn.close()
    assert n == 0
```

- [ ] **Step 2: 跑 tests —— 期望 RED**

```bash
python3 -m pytest tests/test_v5_manual_order_api.py -v
```

Expected:
- `test_preview_returns_full_snapshot` PASS(preview 不改)
- `test_execute_writes_paper_trade` PASS(修改后加 ENABLE_SHORT_TRADING=true,老代码本来就过)
- `test_execute_blocks_short_when_disabled` FAIL(老代码 SHORT 直接过 200,不是 400)
- `test_execute_blocks_when_daily_drawdown_exceeded` FAIL 或 ERROR(老代码 gate_daily_drawdown 未被 import,monkeypatch 找不到属性)

至少 2 个新 test FAIL/ERROR 就够 RED signal。

## GREEN phase

- [ ] **Step 3: `api/routes/v5_manual_order.py` 加 imports**

用 Edit,anchor unique `from api.schemas.v5_manual_order import`.

**Before**:
```python
from api.schemas.v5_manual_order import (
    ManualOrderPreviewRequest, ManualOrderPreviewResponse,
    ManualOrderDecisionSnapshot, ManualOrderRagCase,
    ManualOrderExecuteRequest, ManualOrderExecuteResponse,
)
```

**After**:
```python
from api.schemas.v5_manual_order import (
    ManualOrderPreviewRequest, ManualOrderPreviewResponse,
    ManualOrderDecisionSnapshot, ManualOrderRagCase,
    ManualOrderExecuteRequest, ManualOrderExecuteResponse,
)
from scripts.risk_gates import (
    IronlawViolation, gate_setup_enabled,
    gate_daily_drawdown, gate_per_trade_risk,
    get_today_realized_pnl,
)
from scripts.config import get_config
```

- [ ] **Step 4: 修 `execute` 端点:在 `pm = PaperPositionManager(db_path=_db())` 之前插入 4 层 gate**

Edit anchor unique 是 execute 函数体中 `forced_decision = Decision(` 及其后 blank line 到 `from scripts.paper_position_manager import PaperPositionManager` 那段。

**Before**(节选执行块起始):
```python
    forced_decision = Decision(
        should_trade=True, side=req.side,
        reasoning=decision.reasoning, block_reason=None,
    )

    from scripts.paper_position_manager import PaperPositionManager
    pm = PaperPositionManager(db_path=_db())
    pid = pm.open_position(
```

**After**:
```python
    forced_decision = Decision(
        should_trade=True, side=req.side,
        reasoning=decision.reasoning, block_reason=None,
    )

    # ── M3 铁律层 (Finding 13):manual 与 scorer 走同一层守卫 ──
    # 1. SHORT 全局开关
    if req.side == "SHORT" and not get_config(reload=True).enable_short_trading:
        raise HTTPException(
            status_code=400,
            detail={
                "error_kind": "SHORT_DISABLED",
                "gate": "enable_short_trading",
                "message": "ENABLE_SHORT_TRADING=false;SHORT 交易被全局关闭",
            },
        )

    # 2. Setup 禁用清单
    try:
        from scripts.ai.setup_type import derive_setup_type
        setup_type = derive_setup_type({
            "side": req.side,
            "rsi_15m": indicators.rsi_15m,
            "macd_hist": indicators.macd_hist_15m,
            "macd_hist_prev": indicators.macd_hist_prev_15m,
            "funding_z_score": None,
        })
        gate_setup_enabled(setup_type=setup_type, db_path=_db())
    except IronlawViolation as e:
        raise HTTPException(
            status_code=400,
            detail={"error_kind": e.kind, "gate": "gate_setup_enabled", "message": str(e)},
        )

    # 3. 日熔断
    balance = float(os.environ.get("PAPER_INITIAL_BALANCE_USDT", "1000"))
    try:
        today_pnl = get_today_realized_pnl(_db())
        gate_daily_drawdown(equity_usdt=balance, today_realized_pnl=today_pnl)
    except IronlawViolation as e:
        raise HTTPException(
            status_code=400,
            detail={"error_kind": e.kind, "gate": "gate_daily_drawdown", "message": str(e)},
        )

    # 4. 单笔风险
    from scripts.v5_params import get_param
    cap_pct = float(get_param("v5_risk_per_trade", 0.015, float))
    sl_dist = abs(risk.entry_price - risk.sl_price) * req.sl_multiplier
    sl_dist_pct = sl_dist / max(risk.entry_price, 1e-9)
    final_size = risk.size_usdt * req.size_multiplier
    planned_loss = final_size * risk.leverage * sl_dist_pct
    try:
        gate_per_trade_risk(
            equity_usdt=balance, planned_loss_usdt=planned_loss, cap_pct=cap_pct,
        )
    except IronlawViolation as e:
        raise HTTPException(
            status_code=400,
            detail={"error_kind": e.kind, "gate": "gate_per_trade_risk", "message": str(e)},
        )

    from scripts.paper_position_manager import PaperPositionManager
    pm = PaperPositionManager(db_path=_db())
    pid = pm.open_position(
```

(其他行不动)

- [ ] **Step 5: 跑 tests —— 期望 GREEN**

```bash
python3 -m pytest tests/test_v5_manual_order_api.py -v
```

Expected: 4/4 PASS

若失败:
- `test_execute_writes_paper_trade` FAIL 400 → 检查是否 `get_config(reload=True)` 生效,或 ENABLE_SHORT_TRADING env 正确
- `test_execute_blocks_short_when_disabled` FAIL 200 → 检查 SHORT gate 是否真加了
- `test_execute_blocks_when_daily_drawdown` FAIL 404 monkeypatch attr not found → 检查 gate_daily_drawdown 是否真的以 module-level `from scripts.risk_gates import ..., gate_daily_drawdown, ...` import 到 `api.routes.v5_manual_order` 命名空间(monkeypatch attr 需要在这里)
- 500 → 检查 error_kind/gate 属性访问是否正确

## 邻近回归 + sanity + commit

- [ ] **Step 6: 邻近回归**

```bash
python3 -m pytest tests/test_v5_manual_order_api.py -v
# 无其他 test_v5_manual*.py
```

Expected: 4/4 pass

- [ ] **Step 7: sanity greps**

```bash
grep -c "gate_setup_enabled\|gate_daily_drawdown\|gate_per_trade_risk" api/routes/v5_manual_order.py
# 期望: 6 (3 imports + 3 calls)。若 subagent 用 IronlawViolation 分行 import 可能 3 imports 汇一句 → 4 (1 import 行 + 3 calls) 也 OK,但 spec 用逗号分开的话 grep -c 会数 3 只在 line-level

# 更稳妥的验证:
grep -n "gate_setup_enabled\|gate_daily_drawdown\|gate_per_trade_risk" api/routes/v5_manual_order.py
# 期望: 至少 4 hits(1 import 汇总行 + 3 calls); 更严格看 6 (3 独立 imports + 3 calls) 若分行

grep -c "IronlawViolation" api/routes/v5_manual_order.py
# 期望: ≥4 (1 import + 3 except 块;若合成 1 import 行则 =4)

grep -c "SHORT_DISABLED" api/routes/v5_manual_order.py
# 期望: 1
```

- [ ] **Step 8: Commit**

```bash
git add api/routes/v5_manual_order.py tests/test_v5_manual_order_api.py
git commit -m "$(cat <<'EOF'
fix(v5_manual_order): execute 端点走 M3 铁律 4 层守卫 (Finding 13)

修 bug-fix-list.md Finding 13 (P1):/api/v5/manual-order/execute 之前直接
调 pm.open_position,绕过 scorer 里的 4 层 M3 铁律 gate。运营点 UI 手动
开单可 skip enable_short_trading / setup_enabled / daily_drawdown /
per_trade_risk 全部保护,风控层作为最后一道防线被 side channel 完全无效。

Change:
- imports: IronlawViolation, gate_setup_enabled, gate_daily_drawdown,
  gate_per_trade_risk, get_today_realized_pnl (from scripts.risk_gates)
  + get_config (from scripts.config)
- execute 端点在 pm.open_position 之前顺序跑 4 层 gate:
  1. SHORT 全局: get_config(reload=True).enable_short_trading
  2. Setup: derive_setup_type + gate_setup_enabled
  3. 日熔断: get_today_realized_pnl + gate_daily_drawdown
  4. 单笔: 从 req.multipliers 算 planned_loss + gate_per_trade_risk
- 任一 gate 违规 → HTTPException 400 + detail={error_kind, gate, message}
- 不查 _enable_auto_trading (manual override 就是本意)
- 不改 preview 端点 / _build_full_context / gate_* 内部
- get_config(reload=True) 每次重读 env 避免测试间 singleton 污染

Tests:
- 现有 test_execute_writes_paper_trade 补 ENABLE_SHORT_TRADING=true env
  (老代码 SHORT 免检,现在需要显式开)
- 新增 test_execute_blocks_short_when_disabled: SHORT + enable=false → 400
- 新增 test_execute_blocks_when_daily_drawdown_exceeded: monkeypatch gate
  抛 IronlawViolation → 400 DAILY_DRAWDOWN_HIT
- 每个违规 test 断言 paper_trades 未产生 OPEN 记录

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

# Self-Review 记录

- **spec § 四 imports**: Step 3 ✓
- **spec § 五 4 层 gate**: Step 4 ✓
- **spec § 六 Response 不动**: Step 4 里成功路径未改 ✓
- **spec § 七 tests**: Step 1 (test 1 修 + 2 新 tests) ✓
- **spec § 八 验收**: Step 5 (4/4) + Step 7 (sanity) ✓
- **spec § 九 失效模式**: 已声明 env vs DB / cache / funding=None / AI 极端值 ✓
- **placeholder scan**: 无 TBD ✓
- **type consistency**: 4 gate 名 / 4 error_kind / gate 字段一致 ✓
- **测试 RED→GREEN**: Step 2 = RED (2 新 tests fail)；Step 5 = GREEN ✓
- **atomicity**: 单 commit at Step 8 ✓
