# Bug Fix Batch 15 · Finding 15 · useV5ActivePositions Promise.allSettled · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `useV5ActivePositions` 用 `Promise.allSettled` 独立降级 live / paper 两路,任一失败不阻塞另一路。`CombinedActive` 加 `live_error?: string` + `paper_error?: string` 让前端后续可显降级提示。

**Architecture:** 单 task TDD:先写 3 tests(RED — 老代码 Promise.all,live 失败 → queryFn reject → data undefined + isError true)→ 改 Promise.allSettled + 加字段 → GREEN → 单 commit。

**Tech Stack:** TypeScript + React + @tanstack/react-query + vitest + @testing-library/react。无新增 npm dep。

## Global Constraints

- Only 2 files touched:
  - `Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts` (modify)
  - `Rabbit Hunterfronted/tests/hooks/useV5ActivePositions.test.ts` (create,3 tests)
- Interface 新字段:`live_error?: string` + `paper_error?: string`(都 optional)
- `Promise.allSettled` 替换 `Promise.all`
- 失败源 fallback `[]` + error 字段填 `reason.message ?? String(reason) ?? 'unknown'`
- 成功源 error 字段 undefined
- 不改 `useV5ClosePosition`(同文件另一 export)
- 不改前端 UI 消费者
- 不改 API 契约
- 不改 refetchInterval(保持 5000ms)
- 现有 tests 无回归:
  - `useV5Signals.test.ts`,`useV5WebSocket.test.ts`,`useV5ManualOrder.test.ts`
- Single commit, subject EXACT: `fix(useV5ActivePositions): Promise.allSettled + live_error/paper_error 降级字段 (Finding 15)`

---

## File Structure

| 路径 | 动作 |
|---|---|
| `Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts` | Modify — Promise.allSettled + interface 加字段 |
| `Rabbit Hunterfronted/tests/hooks/useV5ActivePositions.test.ts` | Create — 3 tests |

---

# Task 1: Promise.allSettled 降级 + 3 tests

**Files:**
- Modify: `Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts`
- Create: `Rabbit Hunterfronted/tests/hooks/useV5ActivePositions.test.ts`

**Interfaces:**
- Consumes: `@tanstack/react-query`, `apiGet` from `../../services/api`, `V5PositionsResponse` / `V5Position` types
- Produces: `CombinedActive` 加 2 可选 error 字段;queryFn 从此永不 reject(除非 Promise.allSettled 自身抛,罕见)

## RED phase

- [ ] **Step 1: 创建 `tests/hooks/useV5ActivePositions.test.ts` 3 tests**

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { renderHook, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import React from 'react';
import { useV5ActivePositions } from '@/hooks/api/useV5ActivePositions';

function makeWrapper() {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return {
    qc,
    wrapper: ({ children }: { children: React.ReactNode }) =>
      React.createElement(QueryClientProvider, { client: qc }, children),
  };
}

const mkPos = (id: number, kind: 'LIVE' | 'PAPER') =>
  ({ id, symbol: `SYM${id}/USDT`, side: 'LONG', status: 'OPEN', _kind: kind } as any);

describe('useV5ActivePositions', () => {
  beforeEach(() => { vi.stubGlobal('fetch', vi.fn()); });
  afterEach(() => { vi.unstubAllGlobals(); });

  it('merges live + paper when both succeed', async () => {
    (fetch as any)
      .mockResolvedValueOnce(new Response(
        JSON.stringify({ status: 'success', data: [mkPos(1, 'LIVE')] }), { status: 200 }))
      .mockResolvedValueOnce(new Response(
        JSON.stringify({ status: 'success', data: [mkPos(2, 'PAPER'), mkPos(3, 'PAPER')] }), { status: 200 }));
    const { wrapper } = makeWrapper();
    const { result } = renderHook(() => useV5ActivePositions(), { wrapper });
    await waitFor(() => expect(result.current.data).toBeTruthy());
    expect(result.current.data!.total).toBe(3);
    expect(result.current.data!.live).toHaveLength(1);
    expect(result.current.data!.paper).toHaveLength(2);
    expect(result.current.data!.live_error).toBeUndefined();
    expect(result.current.data!.paper_error).toBeUndefined();
  });

  it('degrades gracefully when live fails, paper succeeds', async () => {
    (fetch as any)
      .mockRejectedValueOnce(new Error('503 Service Unavailable'))
      .mockResolvedValueOnce(new Response(
        JSON.stringify({ status: 'success', data: [mkPos(1, 'PAPER')] }), { status: 200 }));
    const { wrapper } = makeWrapper();
    const { result } = renderHook(() => useV5ActivePositions(), { wrapper });
    await waitFor(() => expect(result.current.data).toBeTruthy());
    expect(result.current.data!.live).toEqual([]);
    expect(result.current.data!.paper).toHaveLength(1);
    expect(result.current.data!.total).toBe(1);
    expect(result.current.data!.live_error).toContain('503');
    expect(result.current.data!.paper_error).toBeUndefined();
    expect(result.current.isError).toBe(false);
  });

  it('degrades gracefully when paper fails, live succeeds', async () => {
    (fetch as any)
      .mockResolvedValueOnce(new Response(
        JSON.stringify({ status: 'success', data: [mkPos(1, 'LIVE')] }), { status: 200 }))
      .mockRejectedValueOnce(new Error('timeout'));
    const { wrapper } = makeWrapper();
    const { result } = renderHook(() => useV5ActivePositions(), { wrapper });
    await waitFor(() => expect(result.current.data).toBeTruthy());
    expect(result.current.data!.live).toHaveLength(1);
    expect(result.current.data!.paper).toEqual([]);
    expect(result.current.data!.total).toBe(1);
    expect(result.current.data!.live_error).toBeUndefined();
    expect(result.current.data!.paper_error).toContain('timeout');
  });
});
```

- [ ] **Step 2: 跑 tests —— 期望 RED**

```bash
cd "Rabbit Hunterfronted" && npm test -- tests/hooks/useV5ActivePositions.test.ts
```

Expected:
- test 1 可能 pass(两路都成功走 happy path,老 Promise.all 也行,但 live_error / paper_error 字段访问返 undefined 也算 pass 因为 test 断言 undefined)
- test 2 FAIL:live 失败 → 老 Promise.all reject → queryFn 抛 → data 永远 undefined → `expect(result.current.data).toBeTruthy()` timeout
- test 3 FAIL:同理,paper 失败 → data undefined

至少 test 2 + test 3 FAIL 就够 RED。若 test 1 也 fail 说明字段不存在(pass 前需存在于 return)。

## GREEN phase

- [ ] **Step 3: 修 `Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts` interface**

用 Edit,anchor unique `interface CombinedActive {`.

**Before**:
```ts
interface CombinedActive {
  live: V5Position[];
  paper: V5Position[];
  combined: V5Position[];
  total: number;
}
```

**After**:
```ts
interface CombinedActive {
  live: V5Position[];
  paper: V5Position[];
  combined: V5Position[];
  total: number;
  live_error?: string;
  paper_error?: string;
}
```

- [ ] **Step 4: 修 queryFn 用 Promise.allSettled**

用 Edit,anchor unique 是完整 queryFn 函数体。

**Before**:
```ts
    queryFn: async () => {
      const [live, paper] = await Promise.all([
        apiGet<V5PositionsResponse>('/api/v5/positions?status=OPEN'),
        apiGet<V5PositionsResponse>('/api/v5/paper-positions?status=OPEN'),
      ]);
      return {
        live: live.data,
        paper: paper.data,
        combined: [...live.data, ...paper.data],
        total: live.data.length + paper.data.length,
      };
    },
```

**After**:
```ts
    queryFn: async () => {
      const [liveResult, paperResult] = await Promise.allSettled([
        apiGet<V5PositionsResponse>('/api/v5/positions?status=OPEN'),
        apiGet<V5PositionsResponse>('/api/v5/paper-positions?status=OPEN'),
      ]);
      const live = liveResult.status === 'fulfilled' ? liveResult.value.data : [];
      const paper = paperResult.status === 'fulfilled' ? paperResult.value.data : [];
      const live_error = liveResult.status === 'rejected'
        ? String((liveResult.reason as any)?.message ?? liveResult.reason ?? 'unknown')
        : undefined;
      const paper_error = paperResult.status === 'rejected'
        ? String((paperResult.reason as any)?.message ?? paperResult.reason ?? 'unknown')
        : undefined;
      return {
        live,
        paper,
        combined: [...live, ...paper],
        total: live.length + paper.length,
        live_error,
        paper_error,
      };
    },
```

- [ ] **Step 5: 跑 tests —— 期望 GREEN**

```bash
cd "Rabbit Hunterfronted" && npm test -- tests/hooks/useV5ActivePositions.test.ts
```

Expected: 3/3 PASS

若失败:
- test 1 `total !== 3` → 检查 combined 是否正确拼
- test 2 `data.live_error` 不含 '503' → 检查 message 提取
- test 2 `isError == true` → 检查 queryFn 是否真的走了 allSettled 而非 catch throw
- test 3 fetch mockOnce 顺序 → 检查 mockResolvedValueOnce vs mockRejectedValueOnce 顺序(live 先,paper 后)

## 邻近回归 + sanity + commit

- [ ] **Step 6: 邻近 tests 无回归**

```bash
cd "Rabbit Hunterfronted" && npm test -- tests/hooks/
```

Expected:
- `useV5Signals.test.ts` unchanged baseline
- `useV5WebSocket.test.ts` unchanged
- `useV5ManualOrder.test.ts` unchanged
- 新增 `useV5ActivePositions.test.ts` 3/3

- [ ] **Step 7: sanity greps**

```bash
grep -c "Promise.allSettled" "Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts"
# 期望: 1

grep -cE "Promise\.all\b" "Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts"
# 期望: 0 (Promise.all 那种旧调用应全部替换)
# 注意: 用 -E 让 \b 生效; grep -c "Promise.all" 会误匹配 allSettled

grep -c "live_error\|paper_error" "Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts"
# 期望: ≥4 (interface 2 + return block 2)

# useV5ClosePosition 未动
grep -c "useV5ClosePosition" "Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts"
# 期望: 1 (function export 保留)
```

- [ ] **Step 8: Commit**

```bash
git add "Rabbit Hunterfronted/hooks/api/useV5ActivePositions.ts" "Rabbit Hunterfronted/tests/hooks/useV5ActivePositions.test.ts"
git commit -m "$(cat <<'EOF'
fix(useV5ActivePositions): Promise.allSettled + live_error/paper_error 降级字段 (Finding 15)

修 bug-fix-list.md Finding 15 (P1):useV5ActivePositions.queryFn 用
Promise.all 拉 /positions + /paper-positions,任一失败(如 LIVE 端点 503)
整个 Promise.all reject → React Query data=undefined,前端看不到任何仓位,
虽然另一类实际可用。

Change:
- Promise.all → Promise.allSettled 独立降级
- 失败源 fallback [] + reason.message 填 live_error / paper_error 字段
- CombinedActive interface 加 live_error?/paper_error?(可选)
- queryFn 从此不再 reject,isError 保持 false
- 现有消费者(OverviewPage/PortfolioPage/ReliabilityPage/DiagnosticsPage)
  用 active.data?.combined ?? [] 无需改动;后续加降级提示时消费 error 字段

Tests:
- 新增 tests/hooks/useV5ActivePositions.test.ts 3 tests
  - both success → total=3, live/paper 都填
  - live 失败(503) → live=[], paper 正常, live_error 含 '503', isError=false
  - paper 失败(timeout) → paper=[], live 正常, paper_error 含 'timeout'
- useV5Signals/useV5WebSocket/useV5ManualOrder 邻近 tests 无回归

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

# Self-Review 记录

- **spec § 四 Change 1 (interface 加字段)**: Step 3 ✓
- **spec § 五 Change 2 (Promise.allSettled)**: Step 4 ✓
- **spec § 六 Change 3 (3 tests)**: Step 1 ✓
- **spec § 七 验收**: Step 5 (3/3) + Step 6 (回归) + Step 7 (sanity) ✓
- **spec § 八 失效模式**: 已声明双失败 / reason.message 非 string / refetch 自愈 ✓
- **placeholder scan**: 无 TBD ✓
- **type consistency**:
  - 字段名 `live_error` / `paper_error` 一致 ✓
  - `Promise.allSettled` 结果 `.status === 'fulfilled' | 'rejected'` 检查 ✓
- **测试 RED→GREEN**: Step 2 = RED(test 2/3 fail)；Step 5 = GREEN(3/3) ✓
- **atomicity**: 单 commit at Step 8 ✓
